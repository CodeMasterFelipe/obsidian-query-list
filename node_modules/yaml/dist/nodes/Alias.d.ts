import { StringifyContext } from '../stringify/stringify.js';
import { Node, NodeBase } from './Node.js';
import { ToJSContext } from './toJS.js';
export declare namespace Alias {
    interface Parsed extends Alias {
        range: [number, number];
    }
}
export declare class Alias<T extends Node = Node> extends NodeBase {
    source: T;
    constructor(source: T);
    toJSON(arg?: unknown, ctx?: ToJSContext): any;
    toString({ anchors, doc, implicitKey, inStringifyKey }: StringifyContext, _onComment?: () => void, _onChompKeep?: () => void): string;
}
