import type { SourceToken } from '../parse/tokens.js';
export declare function resolveEnd(end: SourceToken[] | undefined, offset: number, reqSpace: boolean, onError: (offset: number, message: string) => void): {
    comment: string;
    offset: number;
};
