import type { Document } from '../doc/Document.js';
import type { SourceToken } from '../parse/tokens.js';
export declare function resolveProps(doc: Document.Parsed, tokens: SourceToken[], startOnNewline: boolean, indicator: 'doc-start' | 'explicit-key-ind' | 'map-value-ind' | 'seq-item-ind', offset: number, onError: (offset: number, message: string) => void): {
    found: {
        indent: number;
        offset: number;
    } | null;
    spaceBefore: boolean;
    comment: string;
    hasNewline: boolean;
    anchor: string;
    tagName: string;
    length: number;
    start: number;
};
